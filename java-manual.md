# Java 基本介绍

# 历史

作为全球最长寿、应用最广泛的编程语言之一，**Java** 的发展史几乎就是半部现代企业级软件架构的演进史。

从最初为了智能家电而生的“失败项目”，到如今统治企业级后端、大数据和 Android 生态的绝对霸主。

## 一、 诞生期：从“绿色计划”到“一次编写，到处运行” (1991 - 1995)

Java 的诞生带有极传奇的戏剧色彩——它最初并不是为了互联网设计的。

*   **1991年（Oak 语言诞生）**：Sun Microsystems（太阳微系统公司）成立了一个名为“Green Project（绿色计划）”的小组，由 **詹姆斯·高斯林（James Gosling，Java 之父）** 领导。他们试图开发一种用于微波炉、电视机机顶盒等智能家电的紧凑型语言，起名为 **Oak**（源于高斯林办公室窗外的一棵橡树）。
*   **1994年（转型互联网）**：智能家电市场在当年过于超前，项目面临夭折。此时万维网（WWW）悄然兴起，高斯林敏锐地发现 Oak 具有跨平台、代码紧凑、安全的特性，极度适合在网络上传输。于是，团队决定全面转向浏览器和互联网。
*   **1995年（正式更名 Java）**：由于 Oak 商标已被注册，团队在喝着爪哇咖啡讨论时灵感爆发，正式将该语言更名为 **Java**，并喊出了至今响彻世界的口号：**"Write Once, Run Anywhere"（一次编写，到处运行）**。

---

## 二、 黄金期：群雄崛起与企业级霸主的奠定 (1996 - 2004)

正式发布后，Java 凭借其 **JVM（Java虚拟机）** 带来的跨平台优势，迅速席卷全球。

*   **1996年（JDK 1.0）**：Sun 发布了 JDK 1.0，标志着 Java 正式商用。
*   **1998年（重大里程碑：Java 2 诞生）**：Sun 发布了 JDK 1.2，并将 Java 改名为 **Java 2**。也是在此阶段，Java 正式拆分为三大著名战线：
    1.  **J2SE (Standard Edition)**：面向桌面与基础开发。
    2.  **J2EE (Enterprise Edition)**：面向企业级分布式后端（这也是后来全天下绝大多数 Java 程序员的饭碗）。
    3.  **J2ME (Micro Edition)**：面向移动端与嵌入式（塞班手机时代的霸主）。
*   **2004年（划时代版本：Java 5.0）**：为了体现版本的巨大跃升，Sun 直接跳过了 1.5 的称呼，将其命名为 **Java 5.0**。这个版本引入了**泛型、注解（Annotation）、枚举（Enum）、自动拆装箱**以及 `java.util.concurrent` 并发包，彻底奠定了 Java 现代化高级语言的根基。

---

## 三、 动荡期：Sun的陨落与甲骨文时代 (2006 - 2014)

一门语言的命运，除了自身的硬实力，还深深受到商业资本帝国的左右。

*   **2006年（完全开源）**：Sun 公司宣布将 Java 捐赠给开源社区，**OpenJDK** 诞生。从此 Java 不再仅仅属于一家商业公司。
*   **2009-2010年（Oracle 世纪大收购）**：由于不擅长商业变现，Sun 公司连年亏损，最终在 2010 年被数据库巨头 **Oracle（甲骨文）** 以 74 亿美元吞并。Java 正式改姓“甲骨文”。
*   **2014年（巅峰里程碑：Java 8）**：这是 Java 历史上最成功、生命周期最长的版本。Oracle 终于推出了 **Java 8**，它带来了**Lambda 表达式、Stream API、新的日期时间 API**。它不仅极大简化了代码编写，还成功帮 Java 挡住了来自 Scala、Kotlin 等现代新秀语言的降维打击。

---

## 四、 现代期：半年一更的飞速进化 (2017 - 至今)

进入云计算时代后，面对 Go、Rust 的轻量化合围，臃肿的 Java 开启了壮士断腕般的自我革新。

*   **2017年（Java 9 与全新发布模式）**：Java 9 引入了模块化系统（Project Jigsaw）。同时，Oracle 做出了一个震惊开源界的决定：**废除以往数年一更的模式，改为每半年（每年的 3 月和 9 月）雷打不动地发布一个大版本**。
*   **LTS（长期支持版）确立**：由于半年一更太快，企业根本跟不上，Java 确立了 LTS 机制。目前的四大核心生产级版本为：
    *   **Java 8**：经典的“钉子户”，至今仍有大量老项目在用。
    *   **Java 11**：首个全面转向云原生优化的 LTS 版本。
    *   **Java 17**：目前新项目极力推荐的黄金平替版本（引入了 Sealed Classes、Records）。
    *   **Java 21**：现代 Java 的“终极完全体”，带来了颠覆并发编程的 **Virtual Threads（虚拟线程/协程）**，让 Java 在面对 Go 的高并发挑战时底气十足。

---

## 🔍 Java 为什么能长盛不衰？

纵观 Java 30 多年的历史，它能始终屹立不倒，核心在于三个特质：

1.  **极强的向后兼容性**：20 年前写的 Java 字节码，拿到今天最新的 Java 21 JVM 上大概率依然能直接跑起来。这种对企业资产的尊重是其他语言罕见的。
2.  **恐怖的生态系统**：Spring 框架、横扫大数据的 Hadoop/Spark 生态、各种无可匹敌的中间件，让 Java 成为了大厂搬砖、解决复杂业务的终极银弹。
3.  **拥抱改变**：从模仿 C++，到引入 Lambda（借鉴函数式编程），再到 Java 21 引入虚拟线程（借鉴 Go 协程），Java 始终在“偷师”业界最新的优秀理念，不断自我重构。


# 数据类型

Java 是一门**强类型**（Strongly Typed）语言，这意味着每一个变量、每一个表达式在编译时都必须有明确、固定的数据类型。

Java 的数据类型整体上可以分为两大类：**基本数据类型**（Primitive Types） 和 **引用数据类型**（Reference Types）。

<!-- more -->

## 一、 基本数据类型 (Primitive Types)

Java 共有 **8 种** 基本数据类型。它们不是对象，直接在**栈内存**（Stack）中存储数值，因此具有极高的性能。

根据数据的性质，这 8 种类型可以分为 4 大类：

### 1. 整数型（4 种）
用于存储没有小数部分的整数。
* **`byte`**：占用 **1 字节**（8位）。范围：`-128` 到 `127`。常用于网络传输或文件二进制流。
* **`short`**：占用 **2 字节**（16位）。范围：`-32,768` 到 `32,767`。如今较少显式使用。
* **`int`**：占用 **4 字节**（32位）。范围：约 ± 21 亿。**Java 中整数的默认类型**。
* **`long`**：占用 **8 字节**（64位）。范围极其庞大。常用于时间戳或大 ID。*注意：声明 long 必须在数字后加 `L` 或 `l`（如 `long num = 100L;`）*。

### 2. 浮点型（2 种）
用于存储带小数点的数字（遵循 IEEE 754 标准）。
* **`float`**：占用 **4 字节**（32位），单精度浮点数。*注意：声明 float 必须在数字后加 `F` 或 `f`（如 `float f = 3.14f;`）*。
* **`double`**：占用 **8 字节**（64位），双精度浮点数。**Java 中浮点数的默认类型**，精度比 `float` 更高。

### 3. 字符型（1 种）
* **`char`**：占用 **2 字节**（16位）。Java 的 `char` 采用 **Unicode** 编码，因此它可以存储单个英文字母，也可以直接存储**单个汉字**（如 `char c = '中';`）。

### 4. 布尔型（1 种）
* **`boolean`**：只有 `true` 和 `false` 两个值。在 Java 中，布尔值不能与 0 或非 0 整数相互转换（与 C/C++ 不同），其占用内存大小在 JVM 规范中未严格定义。

---

## 二、 引用数据类型 (Reference Types)

引用数据类型指向内存中的一个对象。变量本身存放的是对象的**内存地址（引用）**，存储在栈中；而实际的对象数据存储在**堆内存**（Heap）中。

引用类型的默认值统一为 **`nil`（在 Java 中写为 `null`）**。

### 1. 类 (Class)
* **字符串 (`String`)**：Java 中最常用的引用类型。虽然看起来像基本类型，但它是一个由官方封装好的类，具有不可变性（Immutable）。
* **自定义类**：开发者自己通过 `class` 关键字定义的类型（例如 `User`, `Order`）。

### 2. 接口 (Interface)
* 用于定义行为规范（例如 `List`, `Map`）。不能被直接实例化，必须由具体的实现类来实现。

### 3. 数组 (Array)
* 存储**相同类型、固定长度**的数据序列。无论是基本类型的数组（如 `int[]`）还是对象数组（如 `String[]`），在 Java 中都属于引用数据类型。

---

## 🔍 进阶核心特质

### 1. 基本数据类型 vs 引用数据类型（速查表）

| 特性         | 基本数据类型                            | 引用数据类型                              |
| :----------- | :-------------------------------------- | :---------------------------------------- |
| **内存位置** | 变量名和实际值都存储在**栈**中          | 变量名和地址在**栈**，实际对象在**堆**    |
| **垃圾回收** | 方法执行完毕后自动从栈中弹出，不触发 GC | 失去引用后，等待 JVM 垃圾回收器（GC）处理 |
| **默认零值** | `0`, `0.0`, `\u0000`, `false`           | 统一为 `null`                             |
| **参数传递** | **值传递**（传递的是数值的副本）        | **值传递**（传递的是内存地址的副本）      |

### 2. 包装类（Wrapper Classes）与自动拆装箱
为了让基本数据类型也能具备对象的特性（如存入集合 List），Java 为每种基本类型都提供了对应的**包装类**：
* `byte` ➡️ `Byte` / `short` ➡️ `Short` / `int` ➡️ `Integer` / `long` ➡️ `Long`
* `float` ➡️ `Float` / `double` ➡️ `Double` / `char` ➡️ `Character` / `boolean` ➡️ `Boolean`

**自动装箱/拆箱**允许我们在代码中直接混用，由编译器在幕后自动转换：
```java
Integer num = 10; // 自动装箱 (int -> Integer)
int n = num;      // 自动拆箱 (Integer -> int)
```

### 3. ⚠️ 浮点数精度丢失陷阱
Java 中的 `float` 和 `double` 无法精确表示所有十进制小数（例如 `0.1 + 0.2` 不等于 `0.3`）。
* **重要生产规范**：在处理**金融、货币计算**或高精度业务时，**严禁使用 float 和 double**，必须使用 **`java.math.BigDecimal`** 类。


## 三、 Java 集合框架（字典、集合、列表）

在 Java 中，“字典、集合、列表”并没有作为内置的基本关键字，而是统一封装在 `java.util` 包下的 **Collections Framework（集合框架）** 中。它们全部是引用数据类型。

### 1. 列表 (List) — 对应 Python 的 list / Go 的 Slice
用于存储**有序的、允许重复**的元素集合。
* **`ArrayList`（最常用）**：基于**动态数组**实现。随机访问（拿索引取值）极快，但往中间插入或删除元素较慢。
* **`LinkedList`**：基于**双向链表**实现。频繁插入、删除时性能更好，但不支持高效的随机访问。

```java
// 声明一个字符串列表
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Go");
String first = list.get(0); // 获取第一个元素
```

### 2. 字典/映射 (Map) — 对应 Python 的 dict / Go 的 Map
用于存储**键值对 (Key-Value)** 的数据结构。Key 不允许重复，Value 可以重复。
* **`HashMap`（最常用）**：基于哈希表实现，存取速度极快（接近 O(1)），但内部元素是无序的。
* **`LinkedHashMap`**：能够记住元素的**插入顺序**。
* **`ConcurrentHashMap`**：**线程安全**的哈希表，在多线程并发开发中属于核心高频类。

```java
// 声明一个字典 (键为String, 值为Integer)
Map<String, Integer> map = new HashMap<>();
map.put("Apple", 10);
map.put("Banana", 20);
int price = map.get("Apple"); // 结果为 10
```

### 3. 集合 (Set) — 对应 Python 的 set
用于存储**无序的、不允许重复**的元素集合。常用于去重或交集、并集判断。
* **`HashSet`（最常用）**：基于 `HashMap` 的 Key 实现，速度极快，但不保证顺序。
* **`TreeSet`**：内部元素会自动按照大小**升序排序**。

```java
// 声明一个去重集合
Set<Integer> set = new HashSet<>();
set.add(1);
set.add(2);
set.add(1); // 重复添加，这行代码不会生效
System.out.println(set.size()); // 输出结果为 2
```
